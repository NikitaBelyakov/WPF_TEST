<UserControl
    x:Class="WPF_TEST.Views.ImageCardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:WPF_TEST.Helpers"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="clr-namespace:WPF_TEST.ViewModel"
    xmlns:views="clr-namespace:WPF_TEST.Views"
    Width="auto"
    Height="auto"
    d:DataContext="{d:DesignInstance Type=helpers:ImageStates}"
    Background="Transparent"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            x:Key="ButtonStyleGreen"
            BasedOn="{StaticResource MaterialDesignRaisedLightButton}"
            TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Offset="0" Color="#28dc5d" />
                        <GradientStop Offset="1" Color="#1ca24e" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#ffffff" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Focusable" Value="False" />
        </Style>
        <Style
            x:Key="ButtonStyleRed"
            BasedOn="{StaticResource MaterialDesignRaisedLightButton}"
            TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Offset="0" Color="#f67062" />
                        <GradientStop Offset="1" Color="#e74c3c" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="#ffffff" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Focusable" Value="False" />
        </Style>
    </UserControl.Resources>
    <Grid Margin="10,0,10,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="430" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="16"
            Foreground="Gray"
            Text="Нет изображения"
            Visibility="{Binding IsLoaded, Converter={StaticResource BooleanToVisibilityConverterInverse}}" />
        <Viewbox
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <Image
                Margin="0"
                Opacity="1"
                Source="{Binding ImageSource}" />
        </Viewbox>
        <Border
            Grid.Row="1"
            Margin="0,10,0,10"
            Padding="20"
            Background="#2d2d2d"
            CornerRadius="16">
            <StackPanel>
                <Border
                    Width="auto"
                    Height="80"
                    Margin="0,0,0,16"
                    Background="#1e1e1e"
                    BorderBrush="#3d3d3d"
                    BorderThickness="1"
                    CornerRadius="12">
                    <TextBox
                        Height="60"
                        Margin="5,5,5,5"
                        Padding="12,8"
                        materialDesign:HintAssist.Foreground="#6d6d6d"
                        materialDesign:HintAssist.Hint="Введите URL изображения..."
                        Background="#1e1e1e"
                        BorderBrush="#1e1e1e"
                        BorderThickness="1"
                        Foreground="#ffffff"
                        Text="{Binding ImageURL, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap" />
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ImageURL}" Value="">
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    AutoReverse="True"
                                                    RepeatBehavior="Forever"
                                                    Storyboard.TargetProperty="Opacity"
                                                    From="1"
                                                    To="0.4"
                                                    Duration="0:0:0.6" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                    <DataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetProperty="Opacity"
                                                    To="1"
                                                    Duration="0:0:0" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>

                <Viewbox Width="30" Height="30">
                    <materialDesign:PackIcon
                        Foreground="LightGreen"
                        Kind="Check"
                        Visibility="{Binding IsLoaded, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Viewbox>

                <StackPanel
                    Margin="0,20,0,0"
                    HorizontalAlignment="Center"
                    Orientation="Vertical">
                    <Button
                        Width="120"
                        Height="40"
                        Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.LoadImageCommand}"
                        CommandParameter="{Binding}"
                        Content="Загрузить"
                        Cursor="Hand"
                        Foreground="White"
                        IsEnabled="{Binding ImageURL, Converter={StaticResource TextToEnableConverter}}"
                        Style="{StaticResource ButtonStyleGreen}" />
                    <Button
                        Width="120"
                        Height="40"
                        Margin="0,20,0,0"
                        Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.StopImageCommand}"
                        CommandParameter="{Binding}"
                        Content="Остановить"
                        Cursor="Hand"
                        Foreground="White"
                        IsEnabled="{Binding IsLoading}"
                        Style="{StaticResource ButtonStyleRed}" />
                </StackPanel>

                <TextBlock
                    Margin="0,12,0,0"
                    HorizontalAlignment="Center"
                    FontSize="12"
                    Foreground="#9d9d9d"
                    Text="{Binding Progress, StringFormat={}{0}%}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
